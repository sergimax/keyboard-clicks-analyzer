/**
 * Version of the Export JSON document shape (`ExportPayload`).
 * Bump when export fields/structure change in a way readers must notice.
 * Independent of `stats.json` `version` and of app semver.
 */
export const EXPORT_SCHEMA_VERSION = 2;
