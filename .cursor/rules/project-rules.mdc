---
description: Basic rules for AI agents working on this project
alwaysApply: true
---

# Project Rules for AI Agents

## Code Style

- Use TypeScript strictly; avoid `any` unless necessary
- Prefer small, focused modules over large “god” files
- Use meaningful names; keep functions small and focused
- Rust collector: keep the hook path minimal, no network, no text logging
- React UI: function components; keep presentational pieces in `src/ui/components/`

## Naming

- Prefer **kebab-case** for file names (e.g. `collector-runner.ts`, `http-app.ts`)
- Keep exported symbols idiomatic (e.g. `runCollectSession` is fine), but keep **file paths** kebab-case
- Avoid oversimplified names for locals and callback params (e.g. `v`, `d`, `c`, `e`); prefer descriptive names like `event`, `stats`, `scanCode`, `sessionStartedAt`

## Commit Messages

Use [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/):

```
<type>[optional scope]: <description>
```

- **feat:** new feature (MINOR)
- **fix:** bug fix (PATCH)
- **docs:** documentation only
- **style:** formatting, no code change
- **refactor:** code change that neither fixes nor adds a feature
- **perf:** performance improvement
- **test:** adding and updating tests
- **chore:** build, CI, tooling
- **BREAKING CHANGE:** use `!` after type/scope or footer (MAJOR)

Example: `feat(live-server): add reset stats endpoint`

## Versioning & Changelog

- Use [Semantic Versioning](https://semver.org/): MAJOR.MINOR.PATCH
- Version lives in `package.json` and `collector/Cargo.toml` (keep them in sync)
- When releasing: bump both versions, update `CHANGELOG.md` (move `[Unreleased]` into a dated `## [X.Y.Z] - YYYY-MM-DD`), update `README.md` version line
- Changelog entries must describe **actual user-facing changes** included in the release (derived from commits/diff since the previous release), not just “bump version”

### Changelog format

- Follow [Keep a Changelog](https://keepachangelog.com/)
- File: `CHANGELOG.md` at project root
- Sections: `### Added`, `### Changed`, `### Deprecated`, `### Removed`, `### Fixed`, `### Security`
- Unreleased work: `## [Unreleased] — X.Y.Z` (no release date until cut)
- Released entry format: `## [X.Y.Z] - YYYY-MM-DD`
- Add new entries at the top (above previous releases)
- Do **not** add filler entries like “version bump for release”; if a release contains no notable changes, do not cut a release (or explicitly document why)

### Version bump rules

- **PATCH** — bug fixes, minor tweaks
- **MINOR** — new features, non-breaking changes
- **MAJOR** — breaking changes (use `BREAKING CHANGE` in commit)

## Editing

- Prefer editing existing files over creating new ones
- Preserve existing formatting and indentation
- After substantive edits, sanity-check with `npm run build:ui` and `npm run report` (and rebuild collector when Rust changes: `npm run build:collector`)

## Project Context

- Local **Windows** tool: physical keyboard press heatmap for deciding which switches to replace first
- Stack: **Node.js + TypeScript** CLI/orchestrator (`tsx`) + thin **Rust** collector (`WH_KEYBOARD_LL`) + **Vite + React** UI
- Privacy-first: offline; no typed text / window titles / layout logging; live HTTP binds **`127.0.0.1` only**
- Counts **physical keys** (scan code + extended flag), not characters — EN/RU layouts share the same key
- Runtime data under `data/` (gitignored): `stats.json`
- Built UI under `dist/ui/` (gitignored)
- Docs: `README.md`, `CHANGELOG.md`
- Follow existing patterns; check README before adding dependencies

## Key Behaviors

- **`npm run build:collector`** — `cargo build --release` into `collector/target/release/collector.exe` (explicit `--target-dir`)
- **`npm run build:ui`** — Vite build of `src/ui` → `dist/ui`
- **`npm run build`** — collector + UI
- **`npm run collect`** — loads/creates `data/stats.json`, spawns Rust collector, parses NDJSON key-down events, flushes stats periodically; serves React UI; Ctrl+C stops and finalizes recording interval
- **Live view** — `http://127.0.0.1:17823/`; polls `GET /api/stats` (~1s); **LIVE** badge; **Reset stats** (`POST /api/reset`); rankings **All time / Today / Last 7 days** with per-block **Copy**
- **Console session timer** — terminal line `session mm:ss · presses N` for the current run only (not shown in the browser); resets with Reset stats
- **`npm run report`** — serves React viewer from disk stats (no Reset); Ctrl+C to stop
- **`npm run reset`** — deletes `data/stats.json`
- **Key aggregation** — `canonicalKey` / aliases in `keymap.ts`; presses (`count`), `repeatCount`, and consecutive transitions go into all-time + `daily` local-day buckets
- **Bursts** — physical first-downs grouped by >1s idle (`stats.bursts.count` / `longest`); avg length and bursts/hour are derived (not stored)
- **Ignore** key-up for counting; OS auto-repeat increments `repeatCount` (physical first-downs stay in `count` / heat / transitions)
- **Heatmap labels** — US QWERTY **positions** for display; not the user’s active layout
- Restart `collect` after pulling code / rebuilding UI so the live server serves the new `dist/ui`
- **Show numpad** — preference in `localStorage` (`kca-show-numpad`)

## Component Structure

- **CLI** — `cli.ts` dispatches `collect` | `report` | `reset`
- **Session runner** — `collector-runner.ts` owns spawn, flush, timers, live server wiring, finalize session
- **Persistence** — `store.ts` (Node I/O for `stats.json`)
- **Shared (browser-safe)** — `shared/` types, dates, format, period, heat
- **Key map** — `keymap.ts` (scan/ext → label + grid cell; aliases)
- **HTTP** — `server/http-app.ts` serves `dist/ui` + `/api/stats` + optional `/api/reset`; wrappers `live-server.ts` / `report-server.ts`
- **React UI** — `ui/` Vite app (`App.tsx`, components, `styles/app.css`)
- **CLI colors** — `style.ts` (respect `NO_COLOR` / TTY)
- **Rust** — `collector/src/main.rs` emits NDJSON `{ sc, ext, t, rep }` (`rep:0` first-down, `rep:1` auto-repeat)

## Project Structure

```
.
├── package.json
├── vite.config.ts
├── tsconfig.json
├── README.md
├── CHANGELOG.md
├── src/
│   ├── cli.ts
│   ├── collector-runner.ts
│   ├── store.ts
│   ├── keymap.ts
│   ├── style.ts
│   ├── shared/            # browser-safe helpers
│   ├── server/            # loopback HTTP (static UI + JSON API)
│   └── ui/                # Vite + React app
├── dist/ui/               # gitignored build output
├── collector/
└── data/                  # gitignored runtime output (stats.json)
```

Prefer extending these modules over adding new top-level packages or frameworks.

## Communication

- Be direct and concise
- Prefer answering questions before making edits when unclear
- Explain non-obvious changes briefly
