[package]
name = "collector"
version = "2.0.0"
edition = "2021"
description = "Local low-level keyboard hook; emits physical key-down events as NDJSON"
publish = false

[dependencies]
windows = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_UI_WindowsAndMessaging",
  "Win32_System_LibraryLoader",
] }
